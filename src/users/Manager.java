package users;

/**
 * 
 * @author Team Cthulhu's Awakening
 */
class Manager extends BasicUser { //Inherit from Salesman, Chef, Driver? How we do that? O.o
    
    public Manager (int userId, String password) {
        super(userId, password);
    }
}
